#include <iostream>
using namespace std;

/*int main()
{
    int n, sum = 0;
    cout << "Enter n: ";
    cin >> n;

    int arr[50][50];

    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < n; j++)
        {
            cin >> arr[i][j];
        }
    }

    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < n; j++)
        {
            if (i > j)
            {
                sum += arr[i][j];
            }
        }
    }

    cout << "Sum of the elements below the main diagonal: " << sum;

    return 0;
}*/